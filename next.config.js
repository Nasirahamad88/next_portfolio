/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig,
module.exports = {
    images: {
      domains: ['121quotes.com', 'stock.adobe.com' ,'t3.ftcdn.net'], // Add the hostname(s) of the external image URLs
    },
  };
